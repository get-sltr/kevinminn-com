# Vault Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a password-protected document vault at `/vault` on kevinminn.com, backed by Cloudflare R2.

**Architecture:** Astro 5 static site gains server-rendered vault pages via `@astrojs/cloudflare` adapter. Existing pages stay prerendered. Vault pages and API endpoints use `export const prerender = false`. Auth uses HMAC-SHA256 signed cookies checked in Astro middleware. Files stored in Cloudflare R2 bucket `km-v`, accessed through authenticated API endpoints.

**Tech Stack:** Astro 5, @astrojs/cloudflare (v12.x for Pages support), Cloudflare R2, Web Crypto API (HMAC-SHA256), Tailwind 3.

---

## File Map

### New Files

| File | Responsibility |
|------|---------------|
| `src/lib/vault/cookie.ts` | Sign/verify HMAC-SHA256 session cookies |
| `src/lib/vault/paths.ts` | Sanitize and normalize R2 key paths |
| `src/middleware.ts` | Auth gate for `/vault` and `/api/vault` routes |
| `src/pages/vault/login.astro` | Login form page |
| `src/pages/vault/index.astro` | Main vault file browser UI |
| `src/pages/api/vault/auth.ts` | POST: verify password, set cookie |
| `src/pages/api/vault/logout.ts` | POST: clear cookie |
| `src/pages/api/vault/list.ts` | GET: list folder contents from R2 |
| `src/pages/api/vault/upload.ts` | POST: upload file to R2 |
| `src/pages/api/vault/download.ts` | GET: stream file from R2 |
| `src/pages/api/vault/delete.ts` | DELETE: remove file or folder from R2 |
| `src/pages/api/vault/folder.ts` | POST: create folder, DELETE: remove folder |
| `src/env.d.ts` | Type declarations for Cloudflare runtime locals |
| `wrangler.toml` | R2 bucket binding and env var declarations |
| `tests/lib/vault/cookie.test.ts` | Tests for cookie signing/verification |
| `tests/lib/vault/paths.test.ts` | Tests for path sanitization |

### Modified Files

| File | Change |
|------|--------|
| `astro.config.mjs` | Add cloudflare adapter |
| `package.json` | Add @astrojs/cloudflare, vitest |
| `public/robots.txt` | Disallow /vault and /api/vault |
| `public/_headers` | Add no-cache, noindex headers for /vault/* |
| `tsconfig.json` | Add types for cloudflare workers |

---

### Task 1: Install Dependencies and Configure Adapter

**Files:**
- Modify: `package.json`
- Modify: `astro.config.mjs`
- Modify: `tsconfig.json`
- Create: `wrangler.toml`
- Create: `src/env.d.ts`

- [ ] **Step 1: Install the Cloudflare adapter**

```bash
npm install @astrojs/cloudflare@^12
```

- [ ] **Step 2: Update astro.config.mjs**

Replace the entire file with:

```javascript
// @ts-check
import { defineConfig } from 'astro/config';
import tailwind from '@astrojs/tailwind';
import cloudflare from '@astrojs/cloudflare';

// https://astro.build/config
export default defineConfig({
  site: 'https://kevinminn.com',
  adapter: cloudflare(),
  integrations: [tailwind({ applyBaseStyles: false })],
  build: {
    inlineStylesheets: 'auto',
  },
  compressHTML: true,
});
```

- [ ] **Step 3: Create wrangler.toml**

```toml
name = "kevinminn-com"
compatibility_date = "2024-12-01"

[[r2_buckets]]
binding = "VAULT_BUCKET"
bucket_name = "km-v"

[vars]
# VAULT_PASSWORD and VAULT_SECRET are set in Cloudflare Pages dashboard
# Do NOT put real values here
```

- [ ] **Step 4: Create src/env.d.ts**

```typescript
/// <reference path="../.astro/types.d.ts" />

type ENV = {
  VAULT_BUCKET: R2Bucket;
  VAULT_PASSWORD: string;
  VAULT_SECRET: string;
};

type Runtime = import('@astrojs/cloudflare').Runtime<ENV>;

declare namespace App {
  interface Locals extends Runtime {}
}
```

- [ ] **Step 5: Update tsconfig.json**

```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"],
  "compilerOptions": {
    "types": ["@cloudflare/workers-types"]
  }
}
```

- [ ] **Step 6: Install Cloudflare worker types**

```bash
npm install --save-dev @cloudflare/workers-types
```

- [ ] **Step 7: Verify the build still works**

```bash
npm run build
```

Expected: Build succeeds. All existing pages are prerendered as before.

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json astro.config.mjs wrangler.toml src/env.d.ts tsconfig.json
git commit -m "feat(vault): add Cloudflare adapter and R2 binding config"
```

---

### Task 2: Set Up Test Runner

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`

- [ ] **Step 1: Install vitest**

```bash
npm install --save-dev vitest
```

- [ ] **Step 2: Create vitest.config.ts**

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['tests/**/*.test.ts'],
  },
});
```

- [ ] **Step 3: Add test script to package.json**

Add to the `"scripts"` section:

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: Verify vitest runs (no tests yet, should exit clean)**

```bash
npm test
```

Expected: "No test files found" or exits 0.

- [ ] **Step 5: Commit**

```bash
git add vitest.config.ts package.json package-lock.json
git commit -m "chore: add vitest test runner"
```

---

### Task 3: Cookie Signing Utilities (TDD)

**Files:**
- Create: `src/lib/vault/cookie.ts`
- Create: `tests/lib/vault/cookie.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `tests/lib/vault/cookie.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { signCookie, verifyCookie } from '../../src/lib/vault/cookie';

const SECRET = 'test-secret-key-at-least-32-chars-long!!';

describe('signCookie', () => {
  it('returns a string with value and signature separated by a dot', async () => {
    const result = await signCookie('session-token', SECRET);
    const parts = result.split('.');
    expect(parts).toHaveLength(2);
    expect(parts[0]).toBe('session-token');
    expect(parts[1]).toBeTruthy();
  });

  it('produces different signatures for different values', async () => {
    const a = await signCookie('value-a', SECRET);
    const b = await signCookie('value-b', SECRET);
    expect(a.split('.')[1]).not.toBe(b.split('.')[1]);
  });

  it('produces different signatures for different secrets', async () => {
    const a = await signCookie('same-value', 'secret-one-that-is-long-enough!!!');
    const b = await signCookie('same-value', 'secret-two-that-is-long-enough!!!');
    expect(a.split('.')[1]).not.toBe(b.split('.')[1]);
  });
});

describe('verifyCookie', () => {
  it('returns the value for a valid signed cookie', async () => {
    const signed = await signCookie('hello', SECRET);
    const result = await verifyCookie(signed, SECRET);
    expect(result).toBe('hello');
  });

  it('returns null for a tampered value', async () => {
    const signed = await signCookie('hello', SECRET);
    const tampered = 'tampered.' + signed.split('.')[1];
    const result = await verifyCookie(tampered, SECRET);
    expect(result).toBeNull();
  });

  it('returns null for a tampered signature', async () => {
    const signed = await signCookie('hello', SECRET);
    const tampered = signed.split('.')[0] + '.badsignature';
    const result = await verifyCookie(tampered, SECRET);
    expect(result).toBeNull();
  });

  it('returns null for malformed input', async () => {
    expect(await verifyCookie('no-dot-here', SECRET)).toBeNull();
    expect(await verifyCookie('', SECRET)).toBeNull();
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test
```

Expected: FAIL - cannot find module `../../src/lib/vault/cookie`

- [ ] **Step 3: Implement cookie.ts**

Create `src/lib/vault/cookie.ts`:

```typescript
const encoder = new TextEncoder();

async function hmac(message: string, secret: string): Promise<string> {
  const key = await crypto.subtle.importKey(
    'raw',
    encoder.encode(secret),
    { name: 'HMAC', hash: 'SHA-256' },
    false,
    ['sign']
  );
  const sig = await crypto.subtle.sign('HMAC', key, encoder.encode(message));
  return btoa(String.fromCharCode(...new Uint8Array(sig)))
    .replace(/\+/g, '-')
    .replace(/\//g, '_')
    .replace(/=+$/, '');
}

export async function signCookie(value: string, secret: string): Promise<string> {
  const signature = await hmac(value, secret);
  return `${value}.${signature}`;
}

export async function verifyCookie(
  signed: string,
  secret: string
): Promise<string | null> {
  const dotIndex = signed.indexOf('.');
  if (dotIndex === -1 || dotIndex === 0) return null;

  const value = signed.slice(0, dotIndex);
  const signature = signed.slice(dotIndex + 1);

  const expected = await hmac(value, secret);
  if (signature !== expected) return null;

  return value;
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test
```

Expected: All 7 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/vault/cookie.ts tests/lib/vault/cookie.test.ts
git commit -m "feat(vault): add HMAC-SHA256 cookie signing utilities"
```

---

### Task 4: Path Sanitization Utilities (TDD)

**Files:**
- Create: `src/lib/vault/paths.ts`
- Create: `tests/lib/vault/paths.test.ts`

- [ ] **Step 1: Write the failing tests**

Create `tests/lib/vault/paths.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { sanitizePath, isValidFilename, parseFolderPrefix } from '../../src/lib/vault/paths';

describe('sanitizePath', () => {
  it('passes through a clean path', () => {
    expect(sanitizePath('legal/contract.pdf')).toBe('legal/contract.pdf');
  });

  it('strips leading slashes', () => {
    expect(sanitizePath('/legal/contract.pdf')).toBe('legal/contract.pdf');
  });

  it('blocks directory traversal', () => {
    expect(sanitizePath('../etc/passwd')).toBeNull();
    expect(sanitizePath('legal/../../etc/passwd')).toBeNull();
    expect(sanitizePath('legal/../../../etc/passwd')).toBeNull();
  });

  it('collapses double slashes', () => {
    expect(sanitizePath('legal//contract.pdf')).toBe('legal/contract.pdf');
  });

  it('returns null for empty string', () => {
    expect(sanitizePath('')).toBeNull();
  });

  it('strips trailing slashes on file paths', () => {
    expect(sanitizePath('legal/contract.pdf/')).toBe('legal/contract.pdf');
  });

  it('handles root-level files', () => {
    expect(sanitizePath('readme.txt')).toBe('readme.txt');
  });
});

describe('isValidFilename', () => {
  it('accepts normal filenames', () => {
    expect(isValidFilename('contract.pdf')).toBe(true);
    expect(isValidFilename('my-file_v2.docx')).toBe(true);
  });

  it('rejects filenames with slashes', () => {
    expect(isValidFilename('path/file.pdf')).toBe(false);
  });

  it('rejects empty string', () => {
    expect(isValidFilename('')).toBe(false);
  });

  it('rejects dot-only names', () => {
    expect(isValidFilename('.')).toBe(false);
    expect(isValidFilename('..')).toBe(false);
  });
});

describe('parseFolderPrefix', () => {
  it('returns empty string for root', () => {
    expect(parseFolderPrefix('')).toBe('');
    expect(parseFolderPrefix(undefined)).toBe('');
  });

  it('ensures trailing slash', () => {
    expect(parseFolderPrefix('legal')).toBe('legal/');
    expect(parseFolderPrefix('legal/')).toBe('legal/');
  });

  it('handles nested folders', () => {
    expect(parseFolderPrefix('legal/contracts')).toBe('legal/contracts/');
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
npm test
```

Expected: FAIL - cannot find module `../../src/lib/vault/paths`

- [ ] **Step 3: Implement paths.ts**

Create `src/lib/vault/paths.ts`:

```typescript
export function sanitizePath(path: string): string | null {
  if (!path) return null;

  let clean = path.replace(/\/+/g, '/').replace(/^\//, '').replace(/\/$/, '');

  if (!clean) return null;

  const segments = clean.split('/');
  if (segments.some((s) => s === '..' || s === '.')) return null;

  return clean;
}

export function isValidFilename(name: string): boolean {
  if (!name || name === '.' || name === '..') return false;
  return !name.includes('/');
}

export function parseFolderPrefix(prefix: string | undefined): string {
  if (!prefix) return '';
  const clean = prefix.replace(/\/+$/, '');
  return clean ? clean + '/' : '';
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
npm test
```

Expected: All 11 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/vault/paths.ts tests/lib/vault/paths.test.ts
git commit -m "feat(vault): add R2 path sanitization utilities"
```

---

### Task 5: Auth Middleware

**Files:**
- Create: `src/middleware.ts`

- [ ] **Step 1: Create the middleware**

Create `src/middleware.ts`:

```typescript
import { defineMiddleware } from 'astro:middleware';
import { verifyCookie } from './lib/vault/cookie';

const PUBLIC_PATHS = ['/vault/login', '/api/vault/auth'];

export const onRequest = defineMiddleware(async (context, next) => {
  const { pathname } = context.url;

  const isVaultRoute =
    pathname.startsWith('/vault') || pathname.startsWith('/api/vault');
  if (!isVaultRoute) return next();

  if (PUBLIC_PATHS.includes(pathname)) return next();

  const cookie = context.cookies.get('vault-session')?.value;
  if (!cookie) {
    return context.redirect('/vault/login');
  }

  const secret = context.locals.runtime.env.VAULT_SECRET;
  const value = await verifyCookie(cookie, secret);
  if (!value) {
    context.cookies.delete('vault-session', { path: '/' });
    return context.redirect('/vault/login');
  }

  return next();
});
```

- [ ] **Step 2: Verify the build**

```bash
npm run build
```

Expected: Build succeeds. Middleware is included in the worker output.

- [ ] **Step 3: Commit**

```bash
git add src/middleware.ts
git commit -m "feat(vault): add auth middleware for vault routes"
```

---

### Task 6: Auth API Endpoint (Login/Logout)

**Files:**
- Create: `src/pages/api/vault/auth.ts`
- Create: `src/pages/api/vault/logout.ts`

- [ ] **Step 1: Create the auth endpoint**

Create `src/pages/api/vault/auth.ts`:

```typescript
import type { APIRoute } from 'astro';
import { signCookie } from '../../../lib/vault/cookie';

export const prerender = false;

export const POST: APIRoute = async ({ request, cookies, locals }) => {
  const form = await request.formData();
  const password = form.get('password');

  if (typeof password !== 'string') {
    return new Response(JSON.stringify({ error: 'Missing password' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const expected = locals.runtime.env.VAULT_PASSWORD;
  if (password !== expected) {
    return new Response(JSON.stringify({ error: 'Wrong password' }), {
      status: 401,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const secret = locals.runtime.env.VAULT_SECRET;
  const token = crypto.randomUUID();
  const signed = await signCookie(token, secret);

  cookies.set('vault-session', signed, {
    path: '/',
    httpOnly: true,
    secure: true,
    sameSite: 'strict',
    maxAge: 60 * 60 * 24 * 7, // 7 days
  });

  return new Response(JSON.stringify({ ok: true }), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 2: Create the logout endpoint**

Create `src/pages/api/vault/logout.ts`:

```typescript
import type { APIRoute } from 'astro';

export const prerender = false;

export const POST: APIRoute = async ({ cookies }) => {
  cookies.delete('vault-session', { path: '/' });
  return new Response(JSON.stringify({ ok: true }), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 3: Verify the build**

```bash
npm run build
```

Expected: Build succeeds with the new API routes.

- [ ] **Step 4: Commit**

```bash
git add src/pages/api/vault/auth.ts src/pages/api/vault/logout.ts
git commit -m "feat(vault): add login and logout API endpoints"
```

---

### Task 7: Login Page

**Files:**
- Create: `src/pages/vault/login.astro`

- [ ] **Step 1: Create the login page**

Create `src/pages/vault/login.astro`:

```astro
---
export const prerender = false;
import '../styles/global.css';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vault — Kevin Minn</title>
    <meta name="robots" content="noindex, nofollow" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="theme-color" content="#F4EFE6" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  </head>
  <body class="vault-login-body">
    <div class="vault-login">
      <div class="vault-login__card">
        <div class="vault-login__eyebrow">Private</div>
        <h1 class="vault-login__title">Vault</h1>
        <p class="vault-login__sub">Enter your password to continue.</p>

        <form id="vault-login-form" class="vault-login__form">
          <div class="vault-login__field">
            <input
              type="password"
              name="password"
              placeholder="Password"
              required
              autocomplete="current-password"
              aria-label="Vault password"
              class="vault-login__input"
              id="vault-password"
            />
          </div>
          <p class="vault-login__error" id="vault-error"></p>
          <button type="submit" class="vault-login__btn" id="vault-submit">
            Enter vault
          </button>
        </form>

        <a href="/" class="vault-login__back">&larr; Back to site</a>
      </div>
    </div>

    <script>
      const form = document.getElementById('vault-login-form') as HTMLFormElement;
      const errorEl = document.getElementById('vault-error') as HTMLParagraphElement;
      const input = document.getElementById('vault-password') as HTMLInputElement;
      const btn = document.getElementById('vault-submit') as HTMLButtonElement;

      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        errorEl.textContent = '';
        btn.disabled = true;
        btn.textContent = '...';

        const formData = new FormData(form);
        const res = await fetch('/api/vault/auth', { method: 'POST', body: formData });

        if (res.ok) {
          window.location.href = '/vault';
        } else {
          errorEl.textContent = 'Wrong password.';
          input.classList.add('shake');
          setTimeout(() => input.classList.remove('shake'), 500);
          btn.disabled = false;
          btn.textContent = 'Enter vault';
          input.focus();
        }
      });
    </script>

    <style>
      .vault-login-body {
        background: #f4efe6;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
      }
      .vault-login__card {
        background: #fbf8f3;
        border: 1px solid #e8e1d2;
        border-radius: 4px;
        padding: 48px 40px;
        max-width: 400px;
        width: 100%;
        text-align: center;
      }
      .vault-login__eyebrow {
        font-family: 'Inter', sans-serif;
        font-size: 10px;
        font-weight: 500;
        letter-spacing: 0.3em;
        color: #b4543e;
        text-transform: uppercase;
        margin-bottom: 16px;
      }
      .vault-login__title {
        font-family: 'Fraunces', serif;
        font-weight: 300;
        font-size: 48px;
        color: #1c1a17;
        letter-spacing: -0.035em;
        margin-bottom: 8px;
      }
      .vault-login__sub {
        font-family: 'Fraunces', serif;
        font-style: italic;
        font-size: 16px;
        color: #4a453d;
        margin-bottom: 32px;
      }
      .vault-login__form {
        text-align: left;
      }
      .vault-login__input {
        width: 100%;
        padding: 14px 16px;
        border: 1px solid #e8e1d2;
        background: #f4efe6;
        font-family: 'Inter', sans-serif;
        font-size: 14px;
        color: #1c1a17;
        border-radius: 999px;
        outline: none;
        transition: border-color 0.2s;
      }
      .vault-login__input:focus {
        border-color: #b4543e;
      }
      .vault-login__input::placeholder {
        color: #8a8479;
      }
      .vault-login__error {
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        color: #b4543e;
        min-height: 20px;
        margin: 12px 0 0;
        text-align: center;
      }
      .vault-login__btn {
        width: 100%;
        margin-top: 12px;
        background: #1c1a17;
        color: #fbf8f3;
        border: 0;
        padding: 14px 20px;
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        font-weight: 500;
        letter-spacing: 0.05em;
        border-radius: 999px;
        cursor: pointer;
        transition: background 0.2s;
      }
      .vault-login__btn:hover {
        background: #b4543e;
      }
      .vault-login__btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
      }
      .vault-login__back {
        display: inline-block;
        margin-top: 24px;
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        font-weight: 500;
        color: #4a453d;
        text-decoration: none;
        transition: color 0.2s;
      }
      .vault-login__back:hover {
        color: #b4543e;
      }
      @keyframes shake {
        0%, 100% { transform: translateX(0); }
        20%, 60% { transform: translateX(-6px); }
        40%, 80% { transform: translateX(6px); }
      }
      .shake {
        animation: shake 0.4s ease;
      }
    </style>
  </body>
</html>
```

- [ ] **Step 2: Verify the build**

```bash
npm run build
```

Expected: Build succeeds. Login page is server-rendered (not prerendered).

- [ ] **Step 3: Commit**

```bash
git add src/pages/vault/login.astro
git commit -m "feat(vault): add login page"
```

---

### Task 8: R2 File Operations API

**Files:**
- Create: `src/pages/api/vault/list.ts`
- Create: `src/pages/api/vault/upload.ts`
- Create: `src/pages/api/vault/download.ts`
- Create: `src/pages/api/vault/delete.ts`
- Create: `src/pages/api/vault/folder.ts`

- [ ] **Step 1: Create the list endpoint**

Create `src/pages/api/vault/list.ts`:

```typescript
import type { APIRoute } from 'astro';
import { parseFolderPrefix } from '../../../lib/vault/paths';

export const prerender = false;

export const GET: APIRoute = async ({ url, locals }) => {
  const bucket = locals.runtime.env.VAULT_BUCKET;
  const prefix = parseFolderPrefix(url.searchParams.get('prefix') || '');

  const listed = await bucket.list({
    prefix: prefix || undefined,
    delimiter: '/',
  });

  const folders = (listed.delimitedPrefixes || []).map((p: string) => {
    const name = p.slice(prefix.length).replace(/\/$/, '');
    return { name, prefix: p };
  });

  const files = listed.objects
    .filter((obj: R2Object) => !obj.key.endsWith('/.folder'))
    .map((obj: R2Object) => ({
      key: obj.key,
      name: obj.key.slice(prefix.length),
      size: obj.size,
      uploaded: obj.uploaded.toISOString(),
      contentType: obj.httpMetadata?.contentType || 'application/octet-stream',
    }));

  return new Response(JSON.stringify({ folders, files, prefix }), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 2: Create the upload endpoint**

Create `src/pages/api/vault/upload.ts`:

```typescript
import type { APIRoute } from 'astro';
import { sanitizePath } from '../../../lib/vault/paths';

export const prerender = false;

export const POST: APIRoute = async ({ request, locals }) => {
  const bucket = locals.runtime.env.VAULT_BUCKET;
  const form = await request.formData();
  const file = form.get('file') as File | null;
  const folder = (form.get('folder') as string) || '';

  if (!file || !file.name) {
    return new Response(JSON.stringify({ error: 'No file provided' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const key = folder ? `${folder}${file.name}` : file.name;
  const safePath = sanitizePath(key);
  if (!safePath) {
    return new Response(JSON.stringify({ error: 'Invalid file path' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  await bucket.put(safePath, file.stream(), {
    httpMetadata: { contentType: file.type || 'application/octet-stream' },
  });

  return new Response(JSON.stringify({ ok: true, key: safePath }), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 3: Create the download endpoint**

Create `src/pages/api/vault/download.ts`:

```typescript
import type { APIRoute } from 'astro';
import { sanitizePath } from '../../../lib/vault/paths';

export const prerender = false;

export const GET: APIRoute = async ({ url, locals }) => {
  const bucket = locals.runtime.env.VAULT_BUCKET;
  const rawKey = url.searchParams.get('key');

  if (!rawKey) {
    return new Response(JSON.stringify({ error: 'Missing key' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const key = sanitizePath(rawKey);
  if (!key) {
    return new Response(JSON.stringify({ error: 'Invalid key' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const object = await bucket.get(key);
  if (!object) {
    return new Response(JSON.stringify({ error: 'File not found' }), {
      status: 404,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const filename = key.split('/').pop() || 'download';
  return new Response(object.body, {
    headers: {
      'Content-Type': object.httpMetadata?.contentType || 'application/octet-stream',
      'Content-Disposition': `attachment; filename="${filename}"`,
      'Content-Length': String(object.size),
    },
  });
};
```

- [ ] **Step 4: Create the delete endpoint**

Create `src/pages/api/vault/delete.ts`:

```typescript
import type { APIRoute } from 'astro';
import { sanitizePath } from '../../../lib/vault/paths';

export const prerender = false;

export const DELETE: APIRoute = async ({ url, locals }) => {
  const bucket = locals.runtime.env.VAULT_BUCKET;
  const rawKey = url.searchParams.get('key');

  if (!rawKey) {
    return new Response(JSON.stringify({ error: 'Missing key' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const key = sanitizePath(rawKey);
  if (!key) {
    return new Response(JSON.stringify({ error: 'Invalid key' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  await bucket.delete(key);

  return new Response(JSON.stringify({ ok: true }), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 5: Create the folder endpoint**

Create `src/pages/api/vault/folder.ts`:

```typescript
import type { APIRoute } from 'astro';
import { sanitizePath, isValidFilename } from '../../../lib/vault/paths';

export const prerender = false;

export const POST: APIRoute = async ({ request, locals }) => {
  const bucket = locals.runtime.env.VAULT_BUCKET;
  const body = await request.json();
  const parent = (body.parent as string) || '';
  const name = body.name as string;

  if (!name || !isValidFilename(name)) {
    return new Response(JSON.stringify({ error: 'Invalid folder name' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const folderKey = parent ? `${parent}${name}/.folder` : `${name}/.folder`;
  const safePath = sanitizePath(folderKey);
  if (!safePath) {
    return new Response(JSON.stringify({ error: 'Invalid path' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  await bucket.put(safePath, new ArrayBuffer(0));

  return new Response(JSON.stringify({ ok: true }), {
    headers: { 'Content-Type': 'application/json' },
  });
};

export const DELETE: APIRoute = async ({ url, locals }) => {
  const bucket = locals.runtime.env.VAULT_BUCKET;
  const prefix = url.searchParams.get('prefix');

  if (!prefix) {
    return new Response(JSON.stringify({ error: 'Missing prefix' }), {
      status: 400,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  const listed = await bucket.list({ prefix });
  const nonMarkerObjects = listed.objects.filter(
    (obj: R2Object) => !obj.key.endsWith('/.folder')
  );

  if (nonMarkerObjects.length > 0) {
    return new Response(JSON.stringify({ error: 'Folder is not empty' }), {
      status: 409,
      headers: { 'Content-Type': 'application/json' },
    });
  }

  for (const obj of listed.objects) {
    await bucket.delete(obj.key);
  }

  return new Response(JSON.stringify({ ok: true }), {
    headers: { 'Content-Type': 'application/json' },
  });
};
```

- [ ] **Step 6: Verify the build**

```bash
npm run build
```

Expected: Build succeeds with all API endpoints.

- [ ] **Step 7: Commit**

```bash
git add src/pages/api/vault/list.ts src/pages/api/vault/upload.ts src/pages/api/vault/download.ts src/pages/api/vault/delete.ts src/pages/api/vault/folder.ts
git commit -m "feat(vault): add R2 file operations API endpoints"
```

---

### Task 9: Vault File Browser UI

**Files:**
- Create: `src/pages/vault/index.astro`

- [ ] **Step 1: Create the vault file browser page**

Create `src/pages/vault/index.astro`:

```astro
---
export const prerender = false;
import '../../styles/global.css';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vault — Kevin Minn</title>
    <meta name="robots" content="noindex, nofollow" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="theme-color" content="#FBF8F3" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  </head>
  <body class="vault-body">
    <div class="vault">
      <!-- Top bar -->
      <header class="vault__header">
        <div class="vault__header-left">
          <h1 class="vault__title">Vault</h1>
          <nav class="vault__breadcrumb" id="breadcrumb"></nav>
        </div>
        <button class="vault__logout" id="logout-btn">Logout</button>
      </header>

      <!-- Action bar -->
      <div class="vault__actions">
        <button class="vault__btn vault__btn--secondary" id="new-folder-btn">+ New folder</button>
        <label class="vault__btn vault__btn--primary">
          Upload
          <input type="file" id="file-input" multiple hidden />
        </label>
        <div class="vault__search-wrap">
          <input type="text" class="vault__search" id="search-input" placeholder="Filter files..." />
        </div>
      </div>

      <!-- Drop zone -->
      <div class="vault__dropzone" id="dropzone">
        <p>Drop files here to upload</p>
      </div>

      <!-- File list -->
      <div class="vault__list" id="file-list">
        <p class="vault__empty">Loading...</p>
      </div>

      <!-- Delete confirmation modal -->
      <div class="vault__modal" id="delete-modal">
        <div class="vault__modal-card">
          <p class="vault__modal-text" id="delete-text">Delete this item?</p>
          <div class="vault__modal-actions">
            <button class="vault__btn vault__btn--secondary" id="delete-cancel">Cancel</button>
            <button class="vault__btn vault__btn--danger" id="delete-confirm">Delete</button>
          </div>
        </div>
      </div>

      <!-- Upload progress -->
      <div class="vault__upload-status" id="upload-status"></div>
    </div>

    <script>
      let currentPrefix = '';
      let deleteTarget: { type: 'file' | 'folder'; key: string } | null = null;
      const listEl = document.getElementById('file-list')!;
      const breadcrumbEl = document.getElementById('breadcrumb')!;
      const searchEl = document.getElementById('search-input') as HTMLInputElement;
      const dropzone = document.getElementById('dropzone')!;
      const fileInput = document.getElementById('file-input') as HTMLInputElement;
      const modal = document.getElementById('delete-modal')!;
      const deleteText = document.getElementById('delete-text')!;
      const uploadStatus = document.getElementById('upload-status')!;

      function formatSize(bytes: number): string {
        if (bytes === 0) return '0 B';
        const k = 1024;
        const sizes = ['B', 'KB', 'MB', 'GB'];
        const i = Math.floor(Math.log(bytes) / Math.log(k));
        return parseFloat((bytes / Math.pow(k, i)).toFixed(1)) + ' ' + sizes[i];
      }

      function formatDate(iso: string): string {
        return new Date(iso).toLocaleDateString('en-US', {
          month: 'short', day: 'numeric', year: 'numeric',
        });
      }

      function fileIcon(contentType: string): string {
        if (contentType.startsWith('image/')) return '\u{1F5BC}';
        if (contentType === 'application/pdf') return '\u{1F4C4}';
        if (contentType.includes('spreadsheet') || contentType.includes('excel')) return '\u{1F4CA}';
        if (contentType.includes('zip') || contentType.includes('compressed')) return '\u{1F4E6}';
        return '\u{1F4CE}';
      }

      function updateBreadcrumb() {
        const parts = currentPrefix.split('/').filter(Boolean);
        let html = '<button class="vault__crumb" data-prefix="">Root</button>';
        let acc = '';
        for (const part of parts) {
          acc += part + '/';
          html += ` <span class="vault__crumb-sep">/</span> <button class="vault__crumb" data-prefix="${acc}">${part}</button>`;
        }
        breadcrumbEl.innerHTML = html;
        breadcrumbEl.querySelectorAll<HTMLButtonElement>('.vault__crumb').forEach((btn) => {
          btn.addEventListener('click', () => {
            currentPrefix = btn.dataset.prefix || '';
            loadFiles();
          });
        });
      }

      async function loadFiles() {
        updateBreadcrumb();
        const res = await fetch(`/api/vault/list?prefix=${encodeURIComponent(currentPrefix)}`);
        const data = await res.json();
        renderList(data.folders, data.files);
      }

      function renderList(folders: Array<{name: string; prefix: string}>, files: Array<{key: string; name: string; size: number; uploaded: string; contentType: string}>) {
        const query = searchEl.value.toLowerCase();
        let html = '';

        for (const folder of folders) {
          if (query && !folder.name.toLowerCase().includes(query)) continue;
          html += `<div class="vault__row vault__row--folder" data-prefix="${folder.prefix}">
            <span class="vault__row-icon">\u{1F4C1}</span>
            <span class="vault__row-name">${folder.name}</span>
            <span class="vault__row-meta"></span>
            <div class="vault__row-actions">
              <button class="vault__row-btn vault__row-btn--del" data-delete-folder="${folder.prefix}" title="Delete folder">&#x2715;</button>
            </div>
          </div>`;
        }

        for (const file of files) {
          if (query && !file.name.toLowerCase().includes(query)) continue;
          html += `<div class="vault__row vault__row--file">
            <span class="vault__row-icon">${fileIcon(file.contentType)}</span>
            <span class="vault__row-name">${file.name}</span>
            <span class="vault__row-meta">${formatSize(file.size)} &middot; ${formatDate(file.uploaded)}</span>
            <div class="vault__row-actions">
              <a class="vault__row-btn" href="/api/vault/download?key=${encodeURIComponent(file.key)}" title="Download">&#x2193;</a>
              <button class="vault__row-btn vault__row-btn--del" data-delete-file="${file.key}" title="Delete">&#x2715;</button>
            </div>
          </div>`;
        }

        if (!html) {
          html = '<p class="vault__empty">No files yet. Upload something or create a folder.</p>';
        }

        listEl.innerHTML = html;

        listEl.querySelectorAll<HTMLDivElement>('.vault__row--folder').forEach((row) => {
          row.addEventListener('click', (e) => {
            if ((e.target as HTMLElement).closest('.vault__row-btn')) return;
            currentPrefix = row.dataset.prefix || '';
            searchEl.value = '';
            loadFiles();
          });
        });

        listEl.querySelectorAll<HTMLButtonElement>('[data-delete-file]').forEach((btn) => {
          btn.addEventListener('click', () => {
            deleteTarget = { type: 'file', key: btn.dataset.deleteFile! };
            deleteText.textContent = `Delete "${btn.dataset.deleteFile!.split('/').pop()}"?`;
            modal.classList.add('is-open');
          });
        });

        listEl.querySelectorAll<HTMLButtonElement>('[data-delete-folder]').forEach((btn) => {
          btn.addEventListener('click', () => {
            deleteTarget = { type: 'folder', key: btn.dataset.deleteFolder! };
            const name = btn.dataset.deleteFolder!.replace(/\/$/, '').split('/').pop();
            deleteText.textContent = `Delete folder "${name}"? It must be empty.`;
            modal.classList.add('is-open');
          });
        });
      }

      async function uploadFiles(files: FileList | File[]) {
        for (const file of Array.from(files)) {
          uploadStatus.textContent = `Uploading ${file.name}...`;
          uploadStatus.classList.add('is-visible');
          const form = new FormData();
          form.append('file', file);
          form.append('folder', currentPrefix);
          const res = await fetch('/api/vault/upload', { method: 'POST', body: form });
          if (!res.ok) {
            uploadStatus.textContent = `Failed to upload ${file.name}`;
            setTimeout(() => uploadStatus.classList.remove('is-visible'), 3000);
            return;
          }
        }
        uploadStatus.textContent = 'Upload complete.';
        setTimeout(() => uploadStatus.classList.remove('is-visible'), 2000);
        loadFiles();
      }

      // File input
      fileInput.addEventListener('change', () => {
        if (fileInput.files?.length) uploadFiles(fileInput.files);
        fileInput.value = '';
      });

      // Drag and drop
      dropzone.addEventListener('dragover', (e) => { e.preventDefault(); dropzone.classList.add('is-drag'); });
      dropzone.addEventListener('dragleave', () => dropzone.classList.remove('is-drag'));
      dropzone.addEventListener('drop', (e) => {
        e.preventDefault();
        dropzone.classList.remove('is-drag');
        if (e.dataTransfer?.files.length) uploadFiles(e.dataTransfer.files);
      });

      // Search
      searchEl.addEventListener('input', () => loadFiles());

      // New folder
      document.getElementById('new-folder-btn')!.addEventListener('click', () => {
        const name = prompt('Folder name:');
        if (!name) return;
        fetch('/api/vault/folder', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ parent: currentPrefix, name }),
        }).then(() => loadFiles());
      });

      // Delete modal
      document.getElementById('delete-cancel')!.addEventListener('click', () => {
        modal.classList.remove('is-open');
        deleteTarget = null;
      });

      document.getElementById('delete-confirm')!.addEventListener('click', async () => {
        if (!deleteTarget) return;
        if (deleteTarget.type === 'file') {
          await fetch(`/api/vault/delete?key=${encodeURIComponent(deleteTarget.key)}`, { method: 'DELETE' });
        } else {
          const res = await fetch(`/api/vault/folder?prefix=${encodeURIComponent(deleteTarget.key)}`, { method: 'DELETE' });
          if (!res.ok) {
            const data = await res.json();
            alert(data.error || 'Could not delete folder.');
          }
        }
        modal.classList.remove('is-open');
        deleteTarget = null;
        loadFiles();
      });

      // Logout
      document.getElementById('logout-btn')!.addEventListener('click', async () => {
        await fetch('/api/vault/logout', { method: 'POST' });
        window.location.href = '/vault/login';
      });

      // Initial load
      loadFiles();
    </script>

    <style>
      .vault-body {
        background: #fbf8f3;
        min-height: 100vh;
      }
      .vault {
        max-width: 960px;
        margin: 0 auto;
        padding: 32px 40px;
      }
      .vault__header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        margin-bottom: 32px;
        padding-bottom: 20px;
        border-bottom: 1px solid #e8e1d2;
      }
      .vault__header-left {
        display: flex;
        align-items: baseline;
        gap: 16px;
        flex-wrap: wrap;
      }
      .vault__title {
        font-family: 'Fraunces', serif;
        font-weight: 300;
        font-size: 32px;
        color: #1c1a17;
        letter-spacing: -0.02em;
      }
      .vault__breadcrumb {
        display: flex;
        align-items: center;
        gap: 6px;
        flex-wrap: wrap;
      }
      .vault__crumb {
        background: none;
        border: none;
        cursor: pointer;
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        color: #4a453d;
        padding: 0;
        transition: color 0.15s;
      }
      .vault__crumb:hover { color: #b4543e; }
      .vault__crumb-sep {
        font-size: 12px;
        color: #8a8479;
      }
      .vault__logout {
        background: none;
        border: 1px solid #e8e1d2;
        padding: 8px 16px;
        border-radius: 999px;
        font-family: 'Inter', sans-serif;
        font-size: 12px;
        font-weight: 500;
        color: #4a453d;
        cursor: pointer;
        transition: border-color 0.2s, color 0.2s;
      }
      .vault__logout:hover { border-color: #1c1a17; color: #1c1a17; }

      .vault__actions {
        display: flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 16px;
        flex-wrap: wrap;
      }
      .vault__btn {
        font-family: 'Inter', sans-serif;
        font-size: 12px;
        font-weight: 500;
        padding: 10px 16px;
        border-radius: 999px;
        cursor: pointer;
        border: none;
        transition: all 0.2s;
      }
      .vault__btn--primary {
        background: #1c1a17;
        color: #fbf8f3;
      }
      .vault__btn--primary:hover { background: #b4543e; }
      .vault__btn--secondary {
        background: transparent;
        border: 1px solid #e8e1d2;
        color: #1c1a17;
      }
      .vault__btn--secondary:hover { border-color: #1c1a17; }
      .vault__btn--danger {
        background: #b4543e;
        color: #fbf8f3;
      }
      .vault__btn--danger:hover { background: #8b3a29; }
      .vault__search-wrap { flex: 1; min-width: 160px; }
      .vault__search {
        width: 100%;
        padding: 10px 14px;
        border: 1px solid #e8e1d2;
        background: #f4efe6;
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        color: #1c1a17;
        border-radius: 999px;
        outline: none;
      }
      .vault__search:focus { border-color: #b4543e; }
      .vault__search::placeholder { color: #8a8479; }

      .vault__dropzone {
        border: 2px dashed #e8e1d2;
        border-radius: 4px;
        padding: 20px;
        text-align: center;
        margin-bottom: 16px;
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        color: #8a8479;
        transition: border-color 0.2s, background 0.2s;
      }
      .vault__dropzone.is-drag {
        border-color: #b4543e;
        background: rgba(180, 84, 62, 0.04);
      }

      .vault__list {
        display: flex;
        flex-direction: column;
      }
      .vault__row {
        display: grid;
        grid-template-columns: 28px 1fr auto auto;
        align-items: center;
        gap: 12px;
        padding: 14px 8px;
        border-bottom: 1px solid #e8e1d2;
        transition: background 0.1s;
      }
      .vault__row--folder { cursor: pointer; }
      .vault__row:hover { background: rgba(244, 239, 230, 0.5); }
      .vault__row-icon { font-size: 18px; text-align: center; }
      .vault__row-name {
        font-family: 'Inter', sans-serif;
        font-size: 14px;
        color: #1c1a17;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
      .vault__row-meta {
        font-family: 'Inter', sans-serif;
        font-size: 12px;
        color: #8a8479;
        white-space: nowrap;
      }
      .vault__row-actions {
        display: flex;
        gap: 8px;
        align-items: center;
      }
      .vault__row-btn {
        background: none;
        border: none;
        cursor: pointer;
        font-size: 16px;
        color: #8a8479;
        padding: 4px;
        text-decoration: none;
        transition: color 0.15s;
      }
      .vault__row-btn:hover { color: #1c1a17; }
      .vault__row-btn--del:hover { color: #b4543e; }
      .vault__empty {
        font-family: 'Fraunces', serif;
        font-style: italic;
        font-size: 16px;
        color: #8a8479;
        text-align: center;
        padding: 48px 0;
      }

      .vault__modal {
        display: none;
        position: fixed;
        inset: 0;
        z-index: 300;
        background: rgba(28, 26, 23, 0.4);
        align-items: center;
        justify-content: center;
      }
      .vault__modal.is-open { display: flex; }
      .vault__modal-card {
        background: #fbf8f3;
        border: 1px solid #e8e1d2;
        border-radius: 4px;
        padding: 32px;
        max-width: 400px;
        width: 90%;
        text-align: center;
      }
      .vault__modal-text {
        font-family: 'Fraunces', serif;
        font-size: 18px;
        color: #1c1a17;
        margin-bottom: 24px;
      }
      .vault__modal-actions {
        display: flex;
        gap: 8px;
        justify-content: center;
      }

      .vault__upload-status {
        position: fixed;
        bottom: 24px;
        right: 24px;
        background: #1c1a17;
        color: #fbf8f3;
        font-family: 'Inter', sans-serif;
        font-size: 13px;
        padding: 12px 20px;
        border-radius: 999px;
        opacity: 0;
        transform: translateY(10px);
        transition: opacity 0.3s, transform 0.3s;
        pointer-events: none;
      }
      .vault__upload-status.is-visible {
        opacity: 1;
        transform: translateY(0);
      }

      @media (max-width: 768px) {
        .vault { padding: 20px 16px; }
        .vault__header { flex-direction: column; align-items: flex-start; gap: 12px; }
        .vault__actions { flex-direction: column; align-items: stretch; }
        .vault__search-wrap { min-width: auto; }
        .vault__row {
          grid-template-columns: 28px 1fr auto;
          gap: 8px;
          padding: 12px 4px;
        }
        .vault__row-meta { display: none; }
        .vault__dropzone { display: none; }
      }
    </style>
  </body>
</html>
```

- [ ] **Step 2: Verify the build**

```bash
npm run build
```

Expected: Build succeeds. Vault index page is server-rendered.

- [ ] **Step 3: Commit**

```bash
git add src/pages/vault/index.astro
git commit -m "feat(vault): add file browser UI"
```

---

### Task 10: Security Hardening (robots, headers)

**Files:**
- Modify: `public/robots.txt`
- Modify: `public/_headers`

- [ ] **Step 1: Update robots.txt**

Add the following lines to the end of `public/robots.txt`:

```
Disallow: /vault
Disallow: /api/vault
```

- [ ] **Step 2: Update public/_headers**

Add the following block at the end of `public/_headers`:

```
/vault/*
  X-Robots-Tag: noindex, nofollow
  Cache-Control: private, no-store

/api/vault/*
  X-Robots-Tag: noindex, nofollow
  Cache-Control: private, no-store
```

- [ ] **Step 3: Commit**

```bash
git add public/robots.txt public/_headers
git commit -m "feat(vault): add security headers and robots disallow for vault routes"
```

---

### Task 11: Run Tests and Verify Full Build

**Files:** None (verification only)

- [ ] **Step 1: Run all tests**

```bash
npm test
```

Expected: All cookie and path tests pass (18 tests).

- [ ] **Step 2: Run production build**

```bash
npm run build
```

Expected: Build succeeds. Output includes both prerendered static pages and server-rendered vault pages.

- [ ] **Step 3: Commit all remaining changes and push**

```bash
git add -A
git commit -m "feat(vault): complete vault implementation"
git push
```

---

### Task 12: Cloudflare Dashboard Setup (Manual)

These steps must be done by Kevin in the Cloudflare dashboard before the vault will work in production.

- [ ] **Step 1: Create R2 bucket**

Cloudflare dashboard > R2 > Create bucket > Name: `km-v`

- [ ] **Step 2: Bind R2 bucket to Pages project**

Cloudflare dashboard > Pages > kevinminn-com > Settings > Functions > R2 bucket bindings > Add binding:
- Variable name: `VAULT_BUCKET`
- R2 bucket: `km-v`

- [ ] **Step 3: Set environment variables**

Cloudflare dashboard > Pages > kevinminn-com > Settings > Environment variables:

- `VAULT_PASSWORD`: (choose a strong password)
- `VAULT_SECRET`: (generate a random string, e.g. `openssl rand -base64 32`)

Set these for **Production** environment. Optionally set different values for **Preview**.

- [ ] **Step 4: Trigger a redeploy**

Cloudflare dashboard > Pages > kevinminn-com > Deployments > Retry deployment (or push a commit to trigger).

- [ ] **Step 5: Verify**

1. Go to `https://kevinminn.com/vault` - should redirect to `/vault/login`
2. Enter your password - should load the file browser
3. Create a folder, upload a file, download it, delete it
4. Close the browser, reopen - should still be logged in (7-day cookie)
5. Go to `https://kevinminn.com` - site should work exactly as before
